'use strict';

/**
 * email-confirmation-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::email-confirmation-config.email-confirmation-config');
